#include "main.h"
#include <stdio.h>

/**
* main function which checks the code
 *
 * It always returns 0
*/
int main(void)
{
    int n;

    n = _strlen_recursion("Corbin Coleman");
    printf("%d\n", n );
    return (0);
}